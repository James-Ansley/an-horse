{
  "name": "an-horse",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf build && mkdir build && cp index.html build/index.html && cd blog && npm run build",
    "serve": "cd blog && npm run serve",
    "postinstall": "cd blog && npm install"
  }
}
